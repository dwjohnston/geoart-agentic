# Feature Plan: Page Title

## Dependency graph

```
task_01_direct_page_title
```

Single task, no dependencies.

## Task list

| # | Task | Skill |
|---|---|---|
| 01 | Set document title and add a visible app header, both reading "Geoart 3000" | none (direct implementation — plain UI/markup change, does not fit any node-development skill) |

No node-development skill applies to this task: it touches `index.html` and the top-level `App` layout, not the schema/nodes/algorithm system. Implemented directly per `task_01_direct_page_title.md`.
