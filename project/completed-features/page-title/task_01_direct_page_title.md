# Task 01: Page title

Skill: none (direct implementation)

## Prompt

Set the browser tab title (`index.html` `<title>`) to "Geoart 3000", replacing the placeholder "app".

Add a visible header/title reading "Geoart 3000" to the top of the app UI in `src/application/App.tsx`, above the existing three-column layout (side panel / canvas / side panel). Keep it simple — a single heading element, styled consistently with the rest of the minimal UI (see `App.css`).

No new node types, schema changes, or algorithm changes are involved.
