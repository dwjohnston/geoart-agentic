# Curve Modulator Module

Add a curve modulator module that:
- Contains wave-module as a sub-module
- Renders via connect-the-dots render node
- Renders via points render module node

All requisite nodes already exist.
