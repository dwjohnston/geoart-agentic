# Task 02: Reference Algorithm — linker-module

## Skill: `algorithm`

Write a reference algorithm that demonstrates the `linker-module`.

## Details

- Read the handoff at `projects/features/linker-module/handoffs/module-node.md` for the module's port names
- The algorithm uses **two orbit modules** as point sources
  - One orbit module feeds `pointsFrom`
  - The other orbit module feeds `pointsTo`
- The linker module connects the two sets of points with animated lines
- Use sensible default control values to produce a clear, readable visual
