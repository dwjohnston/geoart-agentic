## Algorithms

You are working in the algorithms directory.

## File Scope

- Read from `src/schema` only
- Write only to `src/algorithms`

## Handoff

Before writing an algorithm, check whether a handoff file exists at `project/features/[featureName]/handoffs/compute-node.md`.

If it exists, use the partial algorithm skeleton and input values from that file as the basis for your node declarations.

<!-- include: projectDocs/node-development/declaring_an_algorithm.md -->

<!-- include: projectDocs/node-development/sensible_defaults.md -->
