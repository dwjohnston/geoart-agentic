Suffix the files created in `src/algorithms/reference/*/*` with `ReferenceGraph` 

This makes it easier to find/avoid when searching in the IDE. 


## node_specific

These reference graphs should be very minimal. They are intended to just demonstrate what the node in question is meant to do. 

